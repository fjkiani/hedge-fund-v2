# Same-origin HTTP via vercel.json rewrites → Render (avoids browser CORS).
# WebSocket still hits Render directly (Vercel rewrites do not proxy WS).
VITE_API_URL=/api/v1
VITE_MONITOR_URL=
VITE_WS_URL=wss://lotto-machine-wr6t.onrender.com/api/v1
